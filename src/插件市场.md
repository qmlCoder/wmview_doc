通过程序本身暴露在window对象上的一些公共 api，我们可以开发插件，拓展程序功能，同时保持程序本体的小体积

程序的插件都会上传到github上，可以在插件项目的release中下载，然后拷贝到程序根目录下的plugs目录下并解压缩，重启wmview便可以使用了

> [!CAUTION]
> 主程序版本更新可能会改变公共api，此时会导致依赖旧api的插件不可用，此时需要将主程序退回到插件可用的版本，或者是等待插件更新

## xyzrender

xyzrender能够用来绘制精美的分子图像，将其作为wmview的插件可以方便的调节参数和视角绘制分子图像
