程序运行时会遇到各种各样的错误导致闪退，这里介绍一下常见的原因

## 程序运行闪退

如果程序运行时直接闪退，大概是因为电脑中缺乏 WebView2 运行时环境，此时安装 WebView2 运行时环境即可

## 文件读取错误

当点击一个文件后还未显示分子结构程序就闪退了，这就说明程序读取文件出错，要么是文件的问题，要么是程序的问题，先自己检查一下文件有没有问题，如果文件没问题就把文件发给我我排查程序错误
